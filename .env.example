# 服务运行端口
PORT=3000

# 数据库文件保存路径 (建议绝对路径，避免 pm2 启动目录不一致导致新建 DB)
DB_PATH=dev.sqlite

# JWT 签名秘钥 (CRITICAL: 上线前必须修改为一段极度复杂的随机字符串)
JWT_SECRET=super-secret-key-change-me-in-production

# 运行环境
NODE_ENV=production
